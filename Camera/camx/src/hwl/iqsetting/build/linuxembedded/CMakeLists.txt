# User Variables
set (SUPPORT_FUNCTION_CALL_TRACE 1)

# NO DATA FOR SECTION: Custom additions (begin)

# Common Include
include (${CAMX_PATH}/build/infrastructure/linuxembedded/common.cmake)
message (" CMAKE_FIND_ROOT_PATH = "${CMAKE_FIND_ROOT_PATH})
message (" CMAKE_LIBRARY_PATH = "${CMAKE_LIBRARY_PATH})
message (" CMAKE_INCLUDE_PATH = "${CMAKE_INCLUDE_PATH})
message (" CMAKE_SYSTEM_INCLUDE_PATH = "${CMAKE_SYSTEM_INCLUDE_PATHi})
# Files and Build Type
add_library( camxiqsetting
    ../../anr10setting.cpp
    ../../asf30setting.cpp
    ../../bls12setting.cpp
    ../../bpsabf40setting.cpp
    ../../bpsgic30setting.cpp
    ../../bpslinearization34setting.cpp
    ../../bpspdpc20setting.cpp
    ../../cac22setting.cpp
    ../../cc13setting.cpp
    ../../cst12setting.cpp
    ../../cv12setting.cpp
    ../../demosaic36setting.cpp
    ../../demosaic37setting.cpp
    ../../demux13setting.cpp
    ../../gamma15setting.cpp
    ../../gamma16setting.cpp
    ../../gra10setting.cpp
    ../../gtm10setting.cpp
    ../../hdr22setting.cpp
    ../../hdr23setting.cpp
    ../../hnr10setting.cpp
    ../../icasetting.cpp
    ../../ifeabf34setting.cpp
    ../../ifebpcbcc50setting.cpp
    ../../ifecc12setting.cpp
    ../../ifehdr20setting.cpp
    ../../ifelinearization33setting.cpp
    ../../ifepdpc11setting.cpp
    ../../ipecs20setting.cpp
    ../../ipe2dlut10setting.cpp
    ../../iqsettingutil.cpp
    ../../lsc34setting.cpp
    ../../ltm13setting.cpp
    ../../pedestal13setting.cpp
    ../../sce11setting.cpp
    ../../tf10setting.cpp
    ../../upscale12setting.cpp
    ../../upscale20setting.cpp
    ../../upscale20setting_misc.cpp
    ../../wb12setting.cpp
    ../../wb13setting.cpp
)

# Libraries to compile
target_link_libraries (camxiqsetting nc)

# Include Paths
include_directories (${CAMX_C_INCLUDES})
include_directories (../..)
include_directories (${CAMX_CDK_PATH}/generated/g_chromatix)
include_directories (${CAMX_CDK_PATH}/generated/g_parser)
include_directories (${CAMX_SYSTEM_PATH}/firmware)
include_directories (${CAMX_SYSTEM_PATH}/nclib)
include_directories (${CAMX_SYSTEM_PATH}/nclib/AutoGenerated)
include_directories (${CAMX_SYSTEM_PATH}/nclib/Logic)
include_directories (${CAMX_SYSTEM_PATH}/nclib/Logic/LRME_helpers)
include_directories (${CAMX_SYSTEM_PATH}/nclib/Logic/WarpAdjust)
#include_directories (${TARGET_OUT_HEADERS}/camx)
include_directories (../../../iqinterpolation)

if(IQSETTING EQUAL "OEM1")
        include_directories (${CAMX_OEM1IQ_PATH}/iqsetting)
else()
        include_directories (../../../iqinterpolation)
endif()

# Dependencies


# Definitions
add_definitions (${CAMX_CFLAGS})
add_definitions (${CAMX_CPPFLAGS})
# NO DATA FOR SECTION: Custom additions (end)

# Auto-Gen
#add_dependencies(camxiqsetting autogen)

if (CONFIG STREQUAL "Debug")
    add_definitions( -g )
endif()

